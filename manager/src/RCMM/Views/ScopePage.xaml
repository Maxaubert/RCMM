<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RCMM.Views.ScopePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:RCMM.Core.ViewModels">
    <Grid Padding="20,12,20,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BackButton" Click="BackButton_Click" Padding="6,2" Background="Transparent" BorderThickness="0">
                <FontIcon Glyph="&#xE72B;" FontSize="14"/>
            </Button>
            <TextBlock Grid.Column="1" x:Name="ScopeTitle" Margin="8,0,0,0" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,12,0,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" PlaceholderText="Search…" TextChanged="SearchBox_TextChanged"/>
            <CheckBox Grid.Column="1" x:Name="HideBuiltInsBox" Content="Hide Windows built-ins"
                      VerticalAlignment="Center" Checked="HideBuiltInsBox_Changed" Unchecked="HideBuiltInsBox_Changed"/>
        </Grid>

        <ListView Grid.Row="2" x:Name="EntriesList" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:EntryRowViewModel">
                    <Grid Padding="6,8" ColumnSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Width="24" Height="24" Source="{x:Bind Icon, Converter={StaticResource ObjectToImageSourceConverter}, Mode=OneWay}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Border CornerRadius="3" Padding="4,1" Background="#22808080"
                                        Visibility="{x:Bind IsBuiltIn, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="Built-in" FontSize="10" Opacity="0.8"/>
                                </Border>
                                <TextBlock Text="{x:Bind DisplayName}" FontSize="14"/>
                            </StackPanel>
                            <TextBlock FontSize="11" Opacity="0.6">
                                <Run Text="{x:Bind Source}"/>
                                <Run Text=" · "/>
                                <Run Text="{x:Bind KindLabel}"/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                            <TextBlock Text="Built into Windows" FontSize="10" Opacity="0.55"
                                       VerticalAlignment="Center"
                                       Visibility="{x:Bind CanHide, Converter={StaticResource InvertBoolToVisibilityConverter}}"/>
                            <ToggleSwitch IsOn="{x:Bind IsHidden, Mode=TwoWay, Converter={StaticResource InvertBoolConverter}}"
                                          IsEnabled="{x:Bind CanHide}"
                                          OnContent="" OffContent=""/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
