bin/
obj/
*.user
.vs/
*.suo
*.nupkg
publish/
dist/
TestResults/
*.binlog
worktrees/
.superpowers/

# Claude Code: track shareable config (settings.json, skills/, rules/),
# but keep machine-local + personal instructions out of version control.
# (.claude/worktrees/ is already covered by the worktrees/ rule above.)
.claude/settings.local.json
CLAUDE.local.md
